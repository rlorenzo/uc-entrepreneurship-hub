node_modules
dist
.vite
.vite-plus
*.log
.DS_Store
.env
.env.local
.fallow/
.impeccable/live/sessions/
.antigravitycli/

# TypeScript incremental build cache (regenerated on every build)
*.tsbuildinfo

# Claude Code — local/transient files only. Shared config (.claude/settings.json,
# hooks, commands) is intentionally left committable.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# AI code-review loop scratch output (see code-review-loop) and tool caches —
# local artifacts, never committed.
agent-code-review.md
agent-review-summary.md
cache/
